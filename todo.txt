*

1) Add directory for working with files   //done
2) Add data transfer limit
3) Add transfer speed limit to users                   //done
4) Add upload location in Google Drive
5) Add progress bar for user in browser


&

1) make homepage, user dashboard and log service for users
2) add signup form
2) add multiple user types





#

1) implement jwt for user authentication
2) check if file is streamed or coming from a file server
3) add work around for streaming files








* remove the progress struct after the process is completed and push it to database as user's history
* when creating multiple process request the first progress id gets same as latest // fix this
        problem may relay on refrencing

* edit registeruser func to add image and email as well to the users